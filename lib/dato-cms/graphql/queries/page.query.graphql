query Page($name: String) {
  page(filter: { name: { eq: $name } }) {
    ...Page
  }
  _site {
    favicon {
      url
      mimeType
    }
    globalSeo {
      ...GlobalSeo
    }
  }
  allSocialLinks {
    ...SocialLink
  }
}
