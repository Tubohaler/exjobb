query Staff {
  allStaffs(
    orderBy: _createdAt_ASC
    filter: { _status: { eq: published }, featuredImage: { exists: "true" } }
  ) {
    id
    name
    email
    featuredImage {
      title
      alt
      ...ResponsiveImageFragment
    }
  }
}
