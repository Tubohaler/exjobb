query Page($name: String) {
  page(filter: { name: { eq: $name } }) {
    ...Page
  }
  header {
    ...Header
  }
  footer {
    ...Footer
  }
  _site {
    ...SiteMeta
  }
}

query PageNames {
  pages: allPages {
    name
  }
}

query SocialLinks {
  socialLinksCollection {
    ...SocialLinksCollection
  }
}
