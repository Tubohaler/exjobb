query Page($urlSlug: String) {
  page(filter: { urlSlug: { eq: $urlSlug } }) {
    ...Page
  }
  header {
    ...Header
  }

  footer {
    ...Footer
  }
  _site {
    ...SiteMeta
  }
}

query PageLinks {
  pages: allPages {
    ...PageLink
  }
}

query SocialLinks {
  socialLinksCollection {
    ...SocialLinksCollection
  }
}
